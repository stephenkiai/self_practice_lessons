#include <stdio.h>
/*
*
*
*
*/
int main()
{
    printf("The size of int in this machine is %lu bytes.\n", sizeof(int));
    printf("The size of char in this machine is %lu bytes.\n", sizeof(char));
    printf("The size of float in this machine is %lu bytes.\n", sizeof(float));
    
    return(0);
}